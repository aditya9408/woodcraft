{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn woodcraft.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/"
  }
}